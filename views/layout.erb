<!doctype html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Sell Stuff!</title>
  <link rel='stylesheet' href='/stylesheets/main.css'>
  <link href='https://fonts.googleapis.com/css?family=Droid+Sans' rel='stylesheet' type='text/css'>
</head>
<header>
  <nav>
      <ul>
        <li><span id="DisplayUser"><% if logged_in? %> Logged in as <%= current_user[:user_name] %></span>
            <% else %><% end %>
        </li>
        <li>
          <% if logged_in? %>
            <form action="/session/delete" method="post"></li>
              <li>
                <input type="hidden" value = "delete" name="_method">
                <button>LOGOUT</button>
              </li>
              <li>
                <a href="/user_home">My Profile |</a>
              </li>
            </form>
            <% else %>
              <a href="/session/new">login |</a>
            <% end %>
        <li><a href="/signup">Sign-Up |</a></li>
        <li><a href="/">Main-Page |</a></li>
        <li><a href="/todo">to do </a></li>
        <li><% if logged_in? %>
          <a href="/messages">| Messages </a></li>

          <% else %> <% end %>

          <% if Message.where(receiver_id: current_user).where(read_status: false).count == 0 %>
          <% else %>
          <li id ="msgCt"><%= Message.where(receiver_id: current_user).where(read_status: false).count %></li>

          <% end %>

      </ul>
  </nav>
</header>
<body>

  <%= yield %>

</body>
